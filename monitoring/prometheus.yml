global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'iris-classifier'

scrape_configs:
  - job_name: 'iris-api'
    static_configs:
      - targets: ['localhost:8080']
    metrics_path: '/metrics'
    scrape_interval: 5s
